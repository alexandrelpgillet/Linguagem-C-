#include <iostream>
#include <iomanip>
#include <math.h>

using namespace std; 
int main(){

    int NUM, resultado;
    cin>>NUM;

    resultado = sqrt(NUM);

    cout<<"Raiz quadrada de "<<NUM<<" = "<<resultado<<endl;
    return 0;
}