#include <iostream> 
using namespace std;
int main(){
     int N , D , dia , mes , ano , repeticao , votacao;
     repeticao = 0;
     string espaco;
     espaco ="/";
     
     while(cin>>N>>D){
        for(; repeticao<=D  ; repeticao++){
            cin>>dia>>espaco>>mes>>espaco>>ano;
            
            cin>>votacao;
            
 
        }

        if(++votacao>);

     }
}