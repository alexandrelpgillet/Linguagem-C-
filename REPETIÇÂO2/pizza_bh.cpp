#include <iostream> 
using namespace std;
int main(){
     int N , D , dia , mes , ano , repeticao , votacao;
     repeticao = 0;
     string espaco;
     espaco ="/";
     while(cin>>N>>D){
        for(cin>>dia>>espaco>>mes>>espaco>>ano ; repeticao<=D  ; repeticao++){
            cin>>votacao;
            ++votacao;
 
        }

        if(++votacao>)

     }
}